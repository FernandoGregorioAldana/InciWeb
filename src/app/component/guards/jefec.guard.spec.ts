/*import { TestBed } from '@angular/core/testing';

import { JefecGuard } from './jefec.guard';

describe('JefecGuard', () => {
  let guard: JefecGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(JefecGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
*/